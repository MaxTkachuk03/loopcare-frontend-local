enum UnlockedFeatureType {
  grouping,
  physicalActivities,
  meals,
  assignments,
  allowGroupSessions,
  buddy,
  smartGoals,
  mind,
  unknown,
}

enum UnlockedSubFeatureType {
  sleep,
  calories,
  unknown,
}
