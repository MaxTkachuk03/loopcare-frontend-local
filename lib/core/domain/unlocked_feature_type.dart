enum UnlockedFeatureType {
  grouping,
  physicalActivities,
  meals,
  assignments,
  allowGroupSessions,
  buddy,
  smartGoals,
  unknown,
}

enum UnlockedSubFeatureType {
  sleep,
  calories,
  unknown,
}
